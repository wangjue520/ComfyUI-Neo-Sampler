[project]
name = "comfyui-neo-sampler"
description = "Replicates Forge Neo (neo branch) sampling, RNG and prompt encoding inside ComfyUI - same parameters, same results."
version = "1.1.0"
license = { text = "AGPL-3.0-only" }
dependencies = ["lark", "torchsde", "scipy"]

[project.urls]
Repository = "https://github.com/wangjue520/ComfyUI-Neo-Sampler"

[tool.comfy]
PublisherId = "wangjue520"
DisplayName = "ComfyUI Neo Sampler (Forge Neo)"
Icon = ""
